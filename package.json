{
  "dependencies": {},
  "description": "Temporary file generated by the Rush tool",
  "name": "rush-common",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "./docker_build.sh",
    "clean:dangling": "docker container prune -f && docker image prune -f && docker volume prune -f",
    "clean:pnpm-peer-images": "docker rmi -f $(docker images --filter \"reference=pnpm_peer\" -q)",
    "clean": "pnpm clean:pnpm-peer-images && pnpm run clean:dangling"
  }
}




